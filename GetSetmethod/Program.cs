namespace GetSetmethod
{
    public class Program
    {
        static void Main(string[] args)
        {
            //used to give the secuirty or extra protection to the variavle inside a class(fields)
            //private acess specifier
            //deligate used as a reference to a function,pass method as a function argument//actually pass the base address
            public class Person
            {
                //private fields
                private string name();
                private int 
            }
        }
    }
}
